app.accordion.init();
app.select.init();
app.tabs.init();
app.modal.init();
app.validation.init();
app.timer.init();
app.slider.init();
app.scrollToSections.init();
app.scrollTop.init();

new WOW().init();

new LazyLoad();
